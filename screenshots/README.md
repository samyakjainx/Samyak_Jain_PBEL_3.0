# Screenshots Folder

Add screenshots of the running application here:

- `home.png` — the landing/home page
- `upload.png` — the Upload & Screen page
- `ranking.png` — the ranking table view
- `dashboard.png` — the charts/dashboard view

To capture these: run `streamlit run app.py`, walk through each page, and save
screenshots into this folder. They are referenced from the main `README.md`.
